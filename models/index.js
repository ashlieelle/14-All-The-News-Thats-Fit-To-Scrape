// Exports the module.export object containing all models created. 
// *Note* there is no index inside module because that is where you are housing it.)

module.exports = {
    Article: require("./Article"),
    Note: require("./Note")
};